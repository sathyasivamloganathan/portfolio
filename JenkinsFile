pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Install Dependencies') {
            steps {
                bat 'npm install'
            }
        }
        stage('Build') {
            steps {
                bat 'npm run build'
            }
        }
        stage('Configure Git') {
            steps {
                bat 'git config --global user.email "youremail@example.com"'
                bat 'git config --global user.name "Your Name"'
            }
        }
        stage('Clean gh-pages branch') {
            steps {
                bat '''
                    git update-ref -d refs/heads/gh-pages || echo No gh-pages ref to delete
                    git branch -D gh-pages 2>nul || echo No gh-pages branch to delete
                    exit /b 0
                '''
            }
        }
        stage('Deploy to GitHub Pages') {
            steps {
                bat 'npx gh-pages -d dist --branch=gh-pages'
            }
        }

    }
    post {
        failure {
            echo 'Build or deploy failed.'
        }
        success {
            echo 'Build and deploy succeeded!'
        }
    }
}
